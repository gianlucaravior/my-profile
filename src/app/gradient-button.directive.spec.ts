import { GradientButtonDirective } from './gradient-button.directive';

describe('GradientButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GradientButtonDirective();
    expect(directive).toBeTruthy();
  });
});
